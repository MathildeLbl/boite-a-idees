<%= form_with(model: [idea, comment]) do |f| %>
  <div class="mb-2">
    <%= f.label :body, "Votre commentaire" %>
    <%= f.text_area :body, rows: 3, class: "form-control" %>
  </div>
  <%= f.submit "Publier", class: "btn btn-primary" %>
<% end %>

